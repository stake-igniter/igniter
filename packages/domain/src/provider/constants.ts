import { RPCType } from "@igniter/pocket/proto/pocket/shared/service";

export const PROTOCOL_DEFAULT_URL = '{scheme}://{region}-{rm}-{sid}-{protocol}.{domain}';
export const PROTOCOL_DEFAULT_TYPE = RPCType.JSON_RPC;
